server:
  port: 8080

spring:
  data:
    redis:
      host: localhost
      port: 6379
  cloud:
    gateway:
      server:
        webmvc:
          routes:
            - id: downstream-b-route
              uri: http://localhost:8082
              predicates:
                - Path=/api/v2/**